# TutorHub past questions read-only connection
# PharNote iOS does not auto-load this file.
# Set these values in Xcode Run Scheme environment variables,
# or paste the same values into the in-app "기출 DB" debug panel.

PAST_QUESTIONS_SUPABASE_URL=
PAST_QUESTIONS_SUPABASE_ANON_KEY=
